
# =============================================================================
# W.D. GANN TRADING APPLICATION - ENVIRONMENT CONFIGURATION
# =============================================================================

# -----------------------------------------------------------------------------
# ENVIRONMENT
# -----------------------------------------------------------------------------
# Set to "production" for Railway deployment
NODE_ENV=development

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------
# PORT: Railway automatically sets this, but 5000 is the default for local dev
# Note: Railway will override this with $PORT environment variable
PORT=5000

# HOST: Must be 0.0.0.0 for Railway (allows external connections)
HOST=0.0.0.0

# -----------------------------------------------------------------------------
# CORS CONFIGURATION
# -----------------------------------------------------------------------------
# IMPORTANT FOR RAILWAY: Set this to your Railway deployment URL
# Example for Railway: ALLOWED_ORIGINS=https://your-app.up.railway.app
# For local development with multiple origins:
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5000

# For Railway deployment, update to:
# ALLOWED_ORIGINS=https://your-app.up.railway.app

# -----------------------------------------------------------------------------
# YAHOO FINANCE API (Optional)
# -----------------------------------------------------------------------------
# The yahoo-finance2 package works without an API key by default
# If you have a premium API key, uncomment and add it here:
# YAHOO_FINANCE_API_KEY=your_api_key_here

# -----------------------------------------------------------------------------
# OAUTH CONFIGURATION (Optional - Future Enhancement)
# -----------------------------------------------------------------------------
# If implementing OAuth authentication in the future:
# OAUTH_SERVER_URL=https://your-oauth-server.com
# OAUTH_CLIENT_ID=your_client_id
# OAUTH_CLIENT_SECRET=your_client_secret

# -----------------------------------------------------------------------------
# SESSION CONFIGURATION (Optional - Future Enhancement)
# -----------------------------------------------------------------------------
# If implementing session-based authentication:
# SESSION_SECRET=your-random-secret-here-use-strong-value
# SESSION_MAX_AGE=86400000

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION (Optional - Future Enhancement)
# -----------------------------------------------------------------------------
# If adding database support in the future:
# DATABASE_URL=postgresql://user:password@localhost:5432/gann_db

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
# Available levels: error, warn, info, debug, trace
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# RAILWAY DEPLOYMENT NOTES
# -----------------------------------------------------------------------------
# 1. Railway automatically provides: PORT, RAILWAY_ENVIRONMENT, RAILWAY_PROJECT_ID
# 2. You MUST set ALLOWED_ORIGINS to your Railway app URL in production
# 3. Set NODE_ENV=production in Railway dashboard
# 4. All other variables are optional unless you're using specific features
# -----------------------------------------------------------------------------
